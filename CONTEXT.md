# 简阅转档（markdown-to-word）

本地优先的 Markdown 编辑与 DOCX 导出桌面应用。用户在普通文件夹中管理文档与图片资源，应用负责编辑、接近 Word 的预览和可信的导出交付。

## Language

### 工作区

**工作区（Workspace）**:
用户通过「打开文件夹」选定的根目录；一切文件操作都被限制在其内部。
_Avoid_: 项目、目录、文件夹（泛指时）

**工作区条目（Workspace Entry）**:
文件树中的一个节点：目录、文档或图片，由白名单扩展名分类。
_Avoid_: 文件节点、item

**活动文档（Active Document）**:
当前在编辑器中打开的那一个文档；工作区同一时刻至多一个。
_Avoid_: 当前文件、打开的文件

**脏文档（Dirty Document）**:
有未保存修改的活动文档。删除脏文档（或其祖先目录）前必须经过确认，这是唯一需要二次确认的删除。
_Avoid_: 未保存文件、修改中文件

**工作区元数据（Workspace Metadata）**:
`.md2word/workspace.json`，保存工作区默认导出配置与按文档相对路径（正斜杠）键控的逐文档配置。不含 UI 状态（最近打开、上次文档在 localStorage）。
_Avoid_: 项目配置、设置文件

### 文件操作

**回收站删除（Recycle-bin Delete）**:
删除一律移入系统回收站，绝不物理删除；默认无确认弹窗，仅删除对象涉及脏文档时拦截确认。
_Avoid_: 永久删除、移除

**行内重命名（Inline Rename）**:
在树节点原地渲染输入框完成改名（F2 或右键触发，Enter 提交、Esc 放弃），预选中不含扩展名的主干名。新建条目后自动进入此状态。
_Avoid_: 弹窗重命名

**白名单扩展名（Whitelisted Extension）**:
文件树只显示 `.md/.markdown/.txt` 与 `.png/.jpg/.jpeg/.gif/.webp`；重命名不得使文件落出白名单（否则文件会从树中「消失」，予以拒绝）。
_Avoid_: 支持的格式

**相对路径（Relative Path）**:
以工作区根为基准、正斜杠分隔的路径，与工作区元数据的键一致；「复制相对路径」复制的就是它。区别于图片的 **Markdown 引用**——以活动文档为基准的 `![](…)` 引用。
_Avoid_: 路径（不加限定时）
