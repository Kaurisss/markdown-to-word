# 标题一：中文输出验证

这是正文段落，包含中文内容与简单格式。这里是**加粗**与*斜体*的演示。

> 这是一段引用文本，用于验证缩进与中文字体。

---

# 目录生成测试：一级目录项

这一节用于验证导出 Word 时目录中的一级条目。第一个一级标题会作为正文题名，后续一级标题应进入目录。

## 目录生成测试：二级目录项

这一节用于验证目录中的二级条目缩进与字体。默认课程设计格式只收录到二级标题。

### 目录生成测试：三级标题不入目录

这一节用于确认三级标题仍可正常渲染，但默认目录层级不会收录它。

---

## 标题二：内联格式测试

这是一段包含多种内联格式的文本：**粗体**、*斜体*、***粗斜体***、`行内代码`、~~删除线~~、<u>下划线</u>。

这是一个[超链接示例](https://example.com)，用于验证链接样式。

### 标题三：嵌套列表

#### 标题四：四级标题

##### 标题五：五级标题

###### 标题六：六级标题

---

## 无序列表

- 一级列表项 A
- 一级列表项 B
  - 二级列表项 B-1
  - 二级列表项 B-2
    - 三级列表项 B-2-i
- 一级列表项 C

## 有序列表

1. 第一步：准备材料
2. 第二步：混合搅拌
   1. 子步骤 2.1
   2. 子步骤 2.2
3. 第三步：烘烤完成

## 混合列表

- 无序项
  1. 嵌套有序项
  2. 嵌套有序项
- 另一个无序项

---

## 图、表、公式题注测试

图 系统结构示意图

图2 手动编号图题应保持编号

表 课程设计结果汇总表

公式 欧姆定律 U = I × R

这是一段包含半角标点的中文文本, 用于验证导出时逗号、句号、问号?和括号(test)的规范化处理。

---

## 表格测试

表 表格对齐示例

| 左对齐 | 居中对齐 | 右对齐 |
|:-------|:-------:|-------:|
| 单元格 1 | 单元格 2 | 100 |
| A | B | 200 |
| 长文本内容 | 测试 | 300 |

## 带格式的表格

表 带格式的表格示例

| 功能 | 状态 | 说明 |
|------|------|------|
| **加粗** | ✅ | 支持 |
| *斜体* | ✅ | 支持 |
| `代码` | ⚠️ | 部分支持 |

---

## 代码块测试

### Python

```python
def hello(name: str) -> str:
    """返回问候语"""
    return f"你好，{name}！"

print(hello("世界"))
```

### JavaScript

```javascript
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log(fibonacci(10));
```

### 无语言标记

```
这是一段无语言标记的代码块
用于测试默认代码样式
```

---

## 引用块测试

> 这是一级引用。
>
> > 这是嵌套引用。
> >
> > > 三级嵌套引用。

---

## 综合测试段落

这是一个综合测试段落，包含**加粗文字**和*斜体文字*混排，以及`行内代码`和[链接文本](https://example.com)。还有<u>下划线文本</u>和~~删除线文本~~。

段落之间应该有适当的间距。中文标点，。！？和英文标点,.!?都需要正确显示。

---

## 空行与间距测试

上面有一个分隔线。

这里是分隔线之后的内容。

连续两个空行应该产生段落分隔效果。

---

## 边界情况

- 列表项中包含 **加粗** 和 *斜体*
- 引用中包含 `代码` 和 [链接](https://example.com)
- 表格中包含 `行内代码` 和 **加粗**

| 混合格式 | 示例 |
|----------|------|
| **加粗** + *斜体* | ***粗斜体*** |
| `代码` + [链接](https://example.com) | 测试 |
